#include "entree_sortie.h"
#include <stdio.h>

// Ajoute l'extension extension à la suite de la chaîne de caractères nomfichier.
char * ajout_extension_nom_fichier( char * nomfichier, char * extension);

// Demande un nom de fichier à l'utilisateur.
char * demande_nom_fichier(char * extension);
